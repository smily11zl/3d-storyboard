# 05 — 前端生成交互

**What to build:** AI 生成完整前端体验：侧边栏切换为聊天样式（含返回按钮），输入描述提交生成，实时流式显示生成过程日志（agent 文本与工具事件区分），生成中可停止、按钮置灰禁止重复提交，失败显示错误 + 重试按钮，完成后主内容区自动加载新场景（生成中查看器保持上次场景，无场景时提示"生成或提交后可查看"）。

**Blocked by:** 04 — 前端页面结构

**Status:** ready-for-agent

- [ ] ChatPanel：聊天样式（输入框 + 消息列表 + 返回按钮）
- [ ] SSE 流式渲染：agent 消息累积显示 + 工具事件块（如 blender 命令）样式区分
- [ ] 生成中：提交按钮置灰、显示"停止"按钮、状态提示
- [ ] 取消：点停止 → 调 stop 接口 → 聊天显示"已取消"
- [ ] 失败：显示错误信息 + 重试按钮（同描述重新生成）+ 日志路径提示
- [ ] 完成：聊天显示"✅ 生成完成" → 主内容区自动加载生成场景（替换当前）
- [ ] 生成中查看器保持上次场景；无场景时显示提示文案
