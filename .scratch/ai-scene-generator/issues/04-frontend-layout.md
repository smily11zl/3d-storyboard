# 04 — 前端页面结构

**What to build:** 主界面重构成 V2 布局：顶部栏（产品名 + 侧边栏展开/收起按钮 + 设置入口）、左侧边栏（"直接上传" / "AI 生成" 两个选项）、主内容区保持现有查看器。上传模式行为与 V1 完全一致；设置弹窗可用（provider/模型/推理级别/key + 验证 + 掩码显示）。AI 生成入口此时仅占位（点击提示后续切片实现）。

**Blocked by:** 03 — 生成任务核心链路

**Status:** ready-for-agent

- [ ] TopBar：产品名、侧边栏开关（收起时主内容区全宽）、设置入口(⚙)
- [ ] Sidebar：直接上传 / AI 生成 两个选项，当前模式高亮
- [ ] SettingsModal：provider（锁定 DeepSeek）/ 模型 / 推理级别 / key + 保存验证 + 掩码显示
- [ ] App.tsx 布局重组 + store 扩展（设置状态、模式状态、侧边栏状态）
- [ ] 上传模式行为与 V1 一致（回归验证）
- [ ] AI 生成模式显示占位（"输入描述生成场景"提示 + 输入框禁用或提示后续可用）
